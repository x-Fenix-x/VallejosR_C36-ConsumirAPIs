<!DOCTYPE html>
<html lang="es">

<%- include('partials/head', {title: actor.last_name }) %>

    <body>
        <%- include('partials/header') %>
        
            <div class="card col-12 col-md-10 mx-auto mt-3">
                <div class="card-header">
                    <nav class="d-flex justify-content-between" aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/actors">Actores</a></li>
                            <li class="breadcrumb-item"><a href="/actors/detail/<%= actor.id %>">
                                <%= actor.first_name %> <%= actor.last_name %>
                                </a></li>
                            <li class="breadcrumb-item active" aria-current="page">Eliminar</li>
                        </ol>
                    </nav>
                </div>
                <div class="card-body">
                    <h1 class="fs-5">Esta a punto de eliminar a: <b>
                        <%= actor.first_name %> <%= actor.last_name %>
                        </b>. ¿Desea continuar?</h1>
                    <form action="/actors/delete/<%= actor.id %>?_method=DELETE" method="POST">
                        <div class="mt-4">
                            <div class="col-12 d-md-none">
                                <button type="submit" class="btn btn-danger w-100">Borrar actor</button>
                            </div>
                            <div class="col-md-12 d-none d-md-flex justify-content-center">
                                <button class="btn btn-danger btn-sm me-3 col-3" type="submit">Borrar actor</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
    </body>

</html>